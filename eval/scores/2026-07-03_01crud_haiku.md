# Score — 01_crud_equipment_checkout · CHEAP (run 2)

Run: 2026-07-03 · harness Claude Code · model claude-haiku-4-5 · palsync 55bcb7e ·
orch main@55bcb7e · palbuilder main@55bcb7e · workspace test-02-crud-haiku

Scoring method: `palsync validate` + `palsync test` (authoritative floor) + source review of
workflows/console.js, fragments, pal.json, datasets/equipment.json. Live console render not
performed (renders behind CloudPiston login); markup + tool output verified instead — same
method as the prior cheap run.

**Root cause (single blocker):** the `equipment` dataset is defined correctly in
`datasets/equipment.json` (object-wrapped `fields.DatasetField`, correct types, `status` index)
BUT is **not registered in `pal.json` `datasets.entry`** (left as `[]`) and was **never synced**.
`palsync sync-datasets` → `REFUSED: There are no datasets defined in this pal's pal.json`. No
table exists on the server. The agent's own REVIEW.md confirms it: "Dataset provision blocked —
CRITICAL … Application non-functional at runtime … Server rejects push whenever any DataSet entry
is present in pal.json" (git: commit `6db5211 final: revert dataset entry`). Every runtime data
op hits an absent dataset — and `showList()` does NOT null-guard `ds` before
`ds.createFilter()` (console.js:51–52), so the list action throws at runtime rather than no-op'ing.

**Improvement vs prior cheap run (2026-07-02, 1/10):** dataset JSON shape is now correct
(`fields.DatasetField`, not `fields:[]`), and the workflow **passes server validation** —
`pal_test` VALIDATED, 0 notes (prior run FAILED with 4 notes). The floor rose from 1/3 to 3/3.
Runtime data effects still all fail on the same unsolved blocker: getting the dataset provisioned.
The frontier run solved it by embedding the full `Dataset` object INLINE in `pal.json`
`datasets.entry`; Haiku tried multiple entry formats, all rejected, and reverted.

**Global floor**
| # | Check | Criterion | Evidence |
|---|---|---|---|
| G1 | [x] | pal_validate 0 errors | `palsync validate` → VALIDATION PASSED, 0 problems in 6 files |
| G2 | [x] | pal_test workflow VALIDATED, 0 notes | `palsync test` → ✅ console workflow VALIDATED on server, no validation notes |
| G3 | [x] | every §3 link routes | console.js switch handles list/showForm/saveEquipment/deleteEquipment/showCheckout/checkoutEquipment/checkinEquipment; default→list; Cancel→`?action=list`. All 3 §3 screens reachable. (Screen routing works; write-action data submission is broken — see C2/H1.) |

**Console**
| # | Check | Criterion | Evidence |
|---|---|---|---|
| C1 | [ ] | VISUAL Equipment list: PageHeader `Equipment`, striped table, status badges, no emoji | markup correct (h1 Equipment, table, badge-available/badge-checkedOut, no emoji) BUT cannot render — no server table; `showList()` throws on absent dataset (no ds null-guard). Not human-verified. |
| C2 | [ ] | Data effects reflect writes | FAIL — no table on server; writes cannot persist. Compounded: form fields (`name`/`category`/`notes`) are bare `<input>` not in a `<form>`, and Save is `<c:a href="?action=saveEquipment">` (plain GET link, no `ajax-target`) → input values never submitted. Frontier used `<c:a action=… ajax-target="body">`. |

**Happy-path**
| # | Check | Criterion | Evidence |
|---|---|---|---|
| H1 | [ ] | saveEquipment valid → new row | FAIL — insertRecord targets absent dataset; also field values never submitted (href-anchor, no form) so `name` arrives null even when typed |
| H2 | [ ] | checkoutEquipment valid → checkedOut badge + person | FAIL — no table; update unreachable |
| H3 | [ ] | checkinEquipment → available, person cleared | FAIL — no table |
| H4 | [ ] | deleteEquipment → row absent | FAIL — no table |
| H5 | [x] | empty name → form re-renders `Name is required.` | saveEquipment guards `name==null||length==0` → frag="equipmentForm", error="Name is required." (verbatim §4). Path is reachable and needs no dataset; workflow validates on server. Copy verbatim. |

**Total: 4 / 10**

Violations: 0. The agent ran `pal_validate` + `pal_test`, did NOT falsely declare done, and
honestly reported the blocker in REVIEW.md (verdict CHANGES-NEEDED). Copy verbatim §4 — nothing
invented. Frozen files (SPEC/EXECUTION/DESIGN_SYSTEM/COMPONENTS) untouched. Contrast the prior
cheap run's 1 violation (declared complete without a passing pal_test).

Note: two independent defects, either fatal to writes on its own —
(1) dataset never registered in pal.json / never synced → no server table;
(2) form inputs submitted via `<c:a href>` anchor with no wrapping `<form>` and no `ajax-target`
    → field values never reach the workflow.
The frontier run (10/10) avoided both: inline `Dataset` entry in pal.json + `action=`/`ajax-target`.

---
Cost (from .palsync.usage.json — MCP tools only; full transcript token/read/other split not
available this session): 48 MCP calls (14 validate / 24 push / 5 sync_datasets / 4 test / 1 status)
· 24 pal_push · tokens in/out unavailable · ~8 min MCP-activity span.
Note: 24 pushes — identical thrash to the prior cheap run's 24 (frontier converged in 6). 5
sync_datasets attempts, all REFUSED. The loop never converged on dataset provisioning.
