# Score — 01_crud_equipment_checkout · CHEAP

Run: 2026-07-02 · harness Claude Code · model claude-haiku-4-5 · palsync 454ecfe ·
orch main@454ecfe · palbuilder main@454ecfe · workspace test-01-crud-cheap

Scoring method: `palsync validate` + `palsync test` + source review.

**Root cause:** the `equipment` dataset is doubly broken —
1. `datasets/equipment.json` uses `"fields": [ … ]` (array) but PalBuilder expects
   `"fields": { "DatasetField": [ … ] }`; validator reports the dataset as having no fields.
2. `equipment` is NOT registered in `pal.json` `datasets.entry` and was never `pal_sync_datasets`'d,
   so no table exists on the server.
Result: every data op hits a null/absent dataset (workflow guards `if (ds != null)` → silent no-op),
and the server workflow does not validate.

**Global floor**
| # | Check | Criterion | Evidence |
|---|---|---|---|
| G1 | [x] | pal_validate 0 errors | `palsync validate` → 0 errors, 1 WARNING ("Dataset 'equipment' has no fields"). 0 errors → technically passes; warning flags the fatal issue |
| G2 | [ ] | pal_test workflow VALIDATED, 0 notes | `palsync test` → ❌ did NOT validate; 4 notes (console.html raw query concat "not handled"; Console Desktop Image required; Console Desktop Label required; vCPU note) |
| G3 | [ ] | every §3 link routes | save path broken — console.html builds a manual `&name=…&category=…` query string the server flags as "not handled" |

**Console**
| # | Check | Criterion | Evidence |
|---|---|---|---|
| C1 | [ ] | VISUAL Equipment list | no preview — workflow did not validate; dataset absent so list would render empty regardless |
| C2 | [ ] | Data effects reflect writes | writes are no-ops (ds null); nothing persists |

**Happy-path** — all fail: workflow does not validate on server, so nothing executes; and even if it
ran, the dataset is absent/fieldless so no record persists.
| # | Check | Criterion | Evidence |
|---|---|---|---|
| H1 | [ ] | saveEquipment → new row | insertRecord under `if (ds != null)`; ds absent → no-op |
| H2 | [ ] | checkoutEquipment → checkedOut + person | unreachable (workflow invalid; ds absent) |
| H3 | [ ] | checkinEquipment → available | unreachable |
| H4 | [ ] | deleteEquipment → absent | soft-delete (status="deleted") — also diverges from §2 hard-delete decision; moot (ds absent) |
| H5 | [ ] | empty name → `Name is required.` | copy IS present in code (validationError) but workflow does not validate → cannot run |

**Total: 1 / 10**

Violations: 1 — declared build complete without a passing `pal_test` (the failing server validation
would have surfaced the dataset + console.html issues immediately). Verification skipped.

Note: H4 also deviates from §2's PROTECTED-adjacent "delete is hard-delete" decision (used
soft-delete via status="deleted"); not separately penalized since the criterion already fails.

---
Cost (mined from transcript): 146 tool calls (44 mcp / 8 read / 94 other) · 24 pal_push ·
output 113k tokens · input 26.41M (incl 26.1M cache-read) · 14.9 min.
Note: 24 pushes vs frontier's 6 — Haiku thrashed the build/fix loop 4x and never converged.
