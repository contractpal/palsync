# Execution

1. Inspect the existing workspace and determine the complete bounded change.
2. Rename the shared navigation fragment to `fragments/shared/header.html`.
3. Update every literal consumer and the `pal.json` registration/identity.
4. Validate with zero errors and zero warnings, run a server test, run regression, then push.
