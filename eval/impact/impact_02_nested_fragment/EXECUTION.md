# Execution

1. Inspect the target, its direct consumers, and its manifest entry.
2. Rename it to `fragments/components/profile/profile-card.html`.
3. Update both direct literal consumers and `pal.json`; remove the old file and registration.
4. Validate with zero errors and zero warnings, run a server test, run regression, then push.
