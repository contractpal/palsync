[
  {
    "name": "pal_status",
    "title": "Report pal status",
    "description": "Report whether the server is newer than your last pull, and who holds the lock (read-only).",
    "inputSchema": {
      "type": "object",
      "properties": {
        "detail": {
          "type": "string",
          "enum": [
            "summary",
            "normal",
            "full"
          ]
        },
        "maxDiagnostics": {
          "type": "integer",
          "exclusiveMinimum": 0,
          "maximum": 9007199254740991
        },
        "maxBytes": {
          "type": "integer",
          "exclusiveMinimum": 0,
          "maximum": 9007199254740991
        },
        "includePassing": {
          "type": "boolean"
        },
        "includeDebug": {
          "type": "boolean"
        }
      },
      "$schema": "http://json-schema.org/draft-07/schema#"
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true
    },
    "groups": [
      "project"
    ],
    "keywords": [
      "status"
    ]
  },
  {
    "name": "pal_validate",
    "title": "Validate pal code",
    "description": "Check the pal's code OFFLINE (no server/network) for the mistakes that silently break in PalBuilder: invalid workflow JS for the restricted engine, and invalid c:/XHTML markup. Returns each finding's file, line, ERROR/WARNING label, and the fix. Use for diagnosis between edits; pal_push runs the same validation as its gate and refuses on errors.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "detail": {
          "type": "string",
          "enum": [
            "summary",
            "normal",
            "full"
          ]
        },
        "maxDiagnostics": {
          "type": "integer",
          "exclusiveMinimum": 0,
          "maximum": 9007199254740991
        },
        "maxBytes": {
          "type": "integer",
          "exclusiveMinimum": 0,
          "maximum": 9007199254740991
        },
        "includePassing": {
          "type": "boolean"
        },
        "includeDebug": {
          "type": "boolean"
        }
      },
      "$schema": "http://json-schema.org/draft-07/schema#"
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true
    },
    "groups": [
      "project",
      "spec"
    ],
    "keywords": [
      "validate"
    ]
  },
  {
    "name": "pal_testing",
    "title": "Toggle automated testing",
    "description": "Toggle CHIP runtime/self-tests for this session. false skips pal_test/preview/fetch/screenshot/exercise/regression/SEO/tunnel; true resumes. pal_validate and push validation stay on. Resets at session end.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "enabled": {
          "description": "false stops automated runtime/self-tests now; true resumes them. Omit to report the current setting.",
          "type": "boolean"
        }
      },
      "$schema": "http://json-schema.org/draft-07/schema#"
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true
    },
    "groups": [
      "runtime"
    ],
    "keywords": [
      "testing",
      "testing"
    ]
  },
  {
    "name": "pal_test",
    "title": "Compile workflow on server",
    "description": "Validate a workflow ON THE SERVER and return the server's validation notes. Does NOT open a browser by default; pass preview:true only when the user has stopped for human review and wants a live browser preview. The preview URL carries the user's credentials, is NEVER returned to you, and you CANNOT see the rendered page — use pal_screenshot/pal_exercise for agent-visible verification.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "workflow": {
          "type": "string",
          "enum": [
            "console",
            "web",
            "transaction"
          ]
        },
        "workflowName": {
          "type": "string"
        },
        "preview": {
          "type": "boolean"
        },
        "detail": {
          "type": "string",
          "enum": [
            "summary",
            "normal",
            "full"
          ]
        },
        "maxDiagnostics": {
          "type": "integer",
          "exclusiveMinimum": 0,
          "maximum": 9007199254740991
        },
        "maxBytes": {
          "type": "integer",
          "exclusiveMinimum": 0,
          "maximum": 9007199254740991
        },
        "includePassing": {
          "type": "boolean"
        },
        "includeDebug": {
          "type": "boolean"
        }
      },
      "$schema": "http://json-schema.org/draft-07/schema#"
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true
    },
    "groups": [
      "runtime"
    ],
    "keywords": [
      "test",
      "testing"
    ]
  },
  {
    "name": "pal_tunnel_test",
    "title": "Test tunnel workflow",
    "description": "Test a TUNNEL workflow (workflowType 15) by ACTUALLY CALLING it as a web service and returning its JSON response — the one tool where you get real DATA back from the server, not just a validation verdict. REQUIRES askedUser:true, which attests the action/workflow/payload values came from the USER (their message specified them, or they answered your questions) — a call without it returns the questions to relay to the user instead of running. NEVER invent an action or payload. Credentials are minted and refreshed automatically (~5 min expiry). Acts on the LAST PUSHED version — pal_push first. An empty response means the workflow THREW at runtime.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "action": {
          "description": "Action string the workflow dispatches on via request.getAction(). Optional — omitted means the workflow sees null.",
          "type": "string"
        },
        "payload": {
          "description": "JSON payload object — TOP-LEVEL keys are readable in the workflow via request.getPayload().get(key); nested objects do not survive the wire. Default: {}.",
          "type": "object",
          "propertyNames": {
            "type": "string"
          },
          "additionalProperties": {}
        },
        "payloadFile": {
          "description": "Path to a .json file to send as the payload (workspace-relative or absolute). Alternative to payload.",
          "type": "string"
        },
        "workflow": {
          "description": "Tunnel workflow name, with or without .js (default: the pal's registered tunnelServiceWorkflow).",
          "type": "string"
        },
        "askedUser": {
          "description": "REQUIRED attestation: true means the action/workflow/payload values came from the USER in this conversation — their message specified them, or they answered your questions. NEVER set it alongside values you chose yourself.",
          "type": "boolean"
        }
      },
      "$schema": "http://json-schema.org/draft-07/schema#"
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true
    },
    "groups": [
      "runtime"
    ],
    "keywords": [
      "tunnel",
      "test",
      "testing"
    ]
  },
  {
    "name": "pal_debug",
    "title": "Read server debug output",
    "description": "Retrieve the pal's server-side c.debug(...) output — the PalBuilder IDE debug feed; any workflow engine on a test pal writes to it when it executes. CONSUME-ONCE and SHARED: reading clears the buffer for every viewer, including a developer watching PalBuilder's debug view. pal_tunnel_test / pal_fetch / pal_preview (web) / pal_screenshot attach this automatically — call this directly after an opt-in browser preview or a manual run in the user's browser.",
    "inputSchema": {
      "type": "object",
      "properties": {},
      "$schema": "http://json-schema.org/draft-07/schema#"
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true
    },
    "groups": [
      "runtime"
    ],
    "keywords": [
      "debug",
      "testing"
    ]
  },
  {
    "name": "pal_preview",
    "title": "Preview rendered pal",
    "description": "See what the pal RENDERS. WEB pal: pass expect:[strings] for a found/missing verdict (token-efficient default), or selector/maxChars for markup; full HTML otherwise. CONSOLE/transaction pal: does NOT open a browser by default; pass open:true only at a human-review stop. Shows the LAST PUSHED version — pal_push first.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "workflow": {
          "type": "string",
          "enum": [
            "console",
            "web",
            "transaction"
          ]
        },
        "expect": {
          "description": "Strings the rendered page must contain — returns a per-string found/missing verdict instead of the HTML (the default, token-efficient check).",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "selector": {
          "description": "Simple CSS selector (tag/.class/#id) to return only that region's markup.",
          "type": "string"
        },
        "maxChars": {
          "description": "Cap the returned markup to this many characters.",
          "type": "number"
        },
        "open": {
          "description": "Console/transaction only: open the preview in the user's browser. Default false for auto mode.",
          "type": "boolean"
        }
      },
      "$schema": "http://json-schema.org/draft-07/schema#"
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true
    },
    "groups": [
      "browser"
    ],
    "keywords": [
      "preview"
    ]
  },
  {
    "name": "pal_fetch",
    "title": "Fetch rendered web page",
    "description": "Verify ONE page of a WEB pal renders. DEFAULT: pass expect:[strings] for a per-string found/missing verdict WITHOUT the body — the token-efficient post-push check. A successful push does NOT prove render (files missing from pal.json are silently skipped); this does. selector/maxChars return markup instead. path is site-root-relative, e.g. \"about.html\". Shows the LAST PUSHED version.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "description": "Page path relative to the site root, e.g. \"about.html\""
        },
        "expect": {
          "description": "Strings the served page must contain — returns a per-string found/missing verdict instead of the HTML (the default, token-efficient check).",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "selector": {
          "description": "Simple CSS selector (tag/.class/#id) to return only that region's markup.",
          "type": "string"
        },
        "maxChars": {
          "description": "Cap the returned markup to this many characters.",
          "type": "number"
        }
      },
      "required": [
        "path"
      ],
      "$schema": "http://json-schema.org/draft-07/schema#"
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true
    },
    "groups": [
      "browser"
    ],
    "keywords": [
      "fetch"
    ]
  },
  {
    "name": "pal_screenshot",
    "title": "Capture rendered pal screen",
    "description": "Render the last-pushed pal for visual review, detect runtime errors, and return a browser-computed designAudit. Set imageless:true when only the designAudit verdict matters (re-checks after small fixes). Capture page UI at desktop and mobile; require designAudit.errors=0 and inspect pixels. WEB renders directly; CONSOLE/transaction use authenticated replay. Missing browser/auth returns unavailable, never a fake pass.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "page": {
          "description": "Page path under the site root, e.g. \"about.html\" (WEB only). Default: home page.",
          "type": "string"
        },
        "feature": {
          "description": "Human label for the feature or flow being tested. Used to name the .agent-work-history run folder.",
          "type": "string"
        },
        "viewport": {
          "description": "desktop (1280x800, default) or mobile (~390x844).",
          "type": "string",
          "enum": [
            "desktop",
            "mobile"
          ]
        },
        "fullPage": {
          "description": "Capture the whole scroll height, not just the viewport.",
          "type": "boolean"
        },
        "imageless": {
          "description": "Return the designAudit without capturing or returning image data.",
          "type": "boolean"
        }
      },
      "$schema": "http://json-schema.org/draft-07/schema#"
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true
    },
    "groups": [
      "browser"
    ],
    "keywords": [
      "screenshot"
    ]
  },
  {
    "name": "pal_exercise",
    "title": "Exercise pal behavior",
    "description": "Functionally exercise actions end-to-end and assert rendered results — the proof above compile (pal_test) and render (pal_screenshot). Acts on the LAST PUSHED version; push first. Steps run in order and stop at the first failure. WEB uses action+params; CONSOLE/transaction fills inputs then clicks exact text. Scope duplicate controls with within and unique {{runId}} data. Use after create/edit/delete actions.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "steps": {
          "minItems": 1,
          "maxItems": 10,
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "page": {
                "description": "WEB only: page path under the site root to load first, e.g. \"equipment.html\".",
                "type": "string"
              },
              "action": {
                "description": "WEB only: workflow action to invoke, e.g. \"saveEquipment\". Sent as ?action=<name> with params.",
                "type": "string"
              },
              "params": {
                "description": "WEB only: query params sent with the action, e.g. {\"name\":\"Camera\"}.",
                "type": "object",
                "propertyNames": {
                  "type": "string"
                },
                "additionalProperties": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "number"
                    }
                  ]
                }
              },
              "fill": {
                "description": "Fill inputs by their name= attribute before clicking, e.g. {\"name\":\"Camera\",\"category\":\"AV\"}.",
                "type": "object",
                "propertyNames": {
                  "type": "string"
                },
                "additionalProperties": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "number"
                    }
                  ]
                }
              },
              "click": {
                "description": "EXACT visible text of the link/button to click (a c:a Save link), or a simple #id/.class selector.",
                "type": "string"
              },
              "within": {
                "description": "Browser mode: scope click to exactly one CSS/Playwright locator through its identifying cell, e.g. tr:has([data-label=\"Name\"]:has-text(\"{{runId}}\")). Required when the same action text appears in multiple rows/cards; shared-name :has-text() scopes are ambiguous.",
                "type": "string"
              },
              "expect": {
                "description": "Strings that MUST appear in visible rendered text after this step (for example, the saved value in the list). Hidden markup and input value= attributes do not count.",
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "absent": {
                "description": "Full unique strings that must NOT appear in visible rendered text after this step. For edits, use unique old/new values where neither is a substring of the other; a stale old value proves a duplicate insert.",
                "type": "array",
                "items": {
                  "type": "string"
                }
              }
            }
          },
          "description": "Steps run in order; the run stops at the first failing step."
        },
        "workflow": {
          "description": "Engine to exercise (default: auto-detected).",
          "type": "string",
          "enum": [
            "console",
            "web",
            "transaction"
          ]
        },
        "viewport": {
          "description": "Browser-mode viewport (default desktop).",
          "type": "string",
          "enum": [
            "desktop",
            "mobile"
          ]
        }
      },
      "required": [
        "steps"
      ],
      "$schema": "http://json-schema.org/draft-07/schema#"
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true
    },
    "groups": [
      "browser",
      "runtime"
    ],
    "keywords": [
      "exercise",
      "testing"
    ]
  },
  {
    "name": "pal_seo_audit",
    "title": "Audit web page SEO",
    "description": "On-page SEO audit of a WEB pal's server-rendered page (last pushed): title/meta, canonical, og/twitter, single H1, JSON-LD, img alt, robots.txt/sitemap.xml/llms.txt. Returns each problem + its fix, plus what PASSED. Use after pushing a web page; fix every ERROR. Not for console pals. Read the palbuilder-seo skill BEFORE writing heads; this verifies the result.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "detail": {
          "type": "string",
          "enum": [
            "summary",
            "normal",
            "full"
          ]
        },
        "maxDiagnostics": {
          "type": "integer",
          "exclusiveMinimum": 0,
          "maximum": 9007199254740991
        },
        "maxBytes": {
          "type": "integer",
          "exclusiveMinimum": 0,
          "maximum": 9007199254740991
        },
        "includePassing": {
          "type": "boolean"
        },
        "includeDebug": {
          "type": "boolean"
        }
      },
      "$schema": "http://json-schema.org/draft-07/schema#"
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true
    },
    "groups": [
      "browser"
    ],
    "keywords": [
      "seo",
      "audit"
    ]
  },
  {
    "name": "pal_context",
    "title": "Load PalSync context",
    "description": "Load an on-demand PalSync contract section by id or keyword. Use before sync/drift/lock work, creating files or pal.json entries, and dataset provisioning.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "section": {
          "type": "string",
          "enum": [
            "sync-workflow",
            "creating-files",
            "datasets"
          ]
        },
        "query": {
          "description": "Keywords used when the exact section id is unknown.",
          "type": "string"
        }
      },
      "$schema": "http://json-schema.org/draft-07/schema#"
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true
    },
    "groups": [
      "project",
      "spec"
    ],
    "keywords": [
      "context"
    ]
  },
  {
    "name": "pal_spec_lint",
    "title": "Lint pal specification",
    "description": "Lint a SPEC.md OFFLINE for the MECHANICAL half of pal-spec's reality check: placeholders (TBD/decide-later), dead §3 links, §8a primary-key/type/size/indexability against palbuilder-types.md, §5 dataset references, and the §12 floor (plus the REGRESSION criterion when a MAP.md sits beside it). Returns HARD_FLAG/FLAG/NOTE findings; capability->primitive mapping and component checks stay manual.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "spec": {
          "description": "Path to the SPEC.md (default: SPEC.md in the workspace).",
          "type": "string"
        }
      },
      "$schema": "http://json-schema.org/draft-07/schema#"
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true
    },
    "groups": [
      "spec"
    ],
    "keywords": [
      "spec",
      "lint"
    ]
  },
  {
    "name": "pal_regression",
    "title": "Run regression check",
    "description": "Brownfield regression check against baseline/baseline.json (pal-init Step 3). FIRST compares the baseline's mapped marker to the live server — moved => STALE, stops (never verdicts against a stale baseline). Then re-runs validate / pal_test / page-H1 checks vs the baseline, separating CAUSED failures from INHERITED (known_issues) ones; eyeball_only viewports are needs-human, never auto-passed.",
    "inputSchema": {
      "type": "object",
      "properties": {},
      "$schema": "http://json-schema.org/draft-07/schema#"
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true
    },
    "groups": [
      "spec",
      "runtime"
    ],
    "keywords": [
      "regression",
      "testing"
    ]
  },
  {
    "name": "pal_sync_datasets",
    "title": "Synchronize pal datasets",
    "description": "Create or update dataset TABLES on the server from pal.json's dataset definitions (it saves the pal first — editing datasets/<name>.json alone only changes the definition, not the table). Default is a SAFE sync: create if missing, additive changes only, never deletes data. recreate:true DROPS AND REBUILDS a table, DELETING ALL ITS ROWS, and requires a separate exact typed confirmation — it can't happen by accident.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "datasets": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "recreate": {
          "type": "boolean"
        },
        "confirmRecreate": {
          "type": "string"
        },
        "force": {
          "type": "boolean"
        },
        "detail": {
          "type": "string",
          "enum": [
            "summary",
            "normal",
            "full"
          ]
        },
        "maxDiagnostics": {
          "type": "integer",
          "exclusiveMinimum": 0,
          "maximum": 9007199254740991
        },
        "maxBytes": {
          "type": "integer",
          "exclusiveMinimum": 0,
          "maximum": 9007199254740991
        },
        "includePassing": {
          "type": "boolean"
        },
        "includeDebug": {
          "type": "boolean"
        }
      },
      "$schema": "http://json-schema.org/draft-07/schema#"
    },
    "annotations": {
      "readOnlyHint": false,
      "destructiveHint": true,
      "idempotentHint": false
    },
    "groups": [
      "sync"
    ],
    "keywords": [
      "sync",
      "datasets"
    ]
  },
  {
    "name": "pal_pull",
    "title": "Pull pal changes",
    "description": "Pull (sync) the pal from the server. New un-pushed local files are preserved. Refuses if it would overwrite locally-modified server files (use force to override).",
    "inputSchema": {
      "type": "object",
      "properties": {
        "force": {
          "type": "boolean"
        }
      },
      "$schema": "http://json-schema.org/draft-07/schema#"
    },
    "annotations": {
      "readOnlyHint": false,
      "destructiveHint": true,
      "idempotentHint": false
    },
    "groups": [
      "sync",
      "project"
    ],
    "keywords": [
      "pull"
    ]
  },
  {
    "name": "pal_merge",
    "title": "Merge pal changes",
    "description": "Reconcile un-pushed LOCAL changes with SERVER changes since your last pull, keeping BOTH where they don't collide (3-way merge): yours-only stay, server-only are taken, a file BOTH changed is kept as YOURS with the server's beside it as <file>.server to combine by hand. Never overwrites your work silently. Use instead of force push/pull when both sides edited; a clean merge clears drift so pal_push isn't blocked.",
    "inputSchema": {
      "type": "object",
      "properties": {},
      "$schema": "http://json-schema.org/draft-07/schema#"
    },
    "annotations": {
      "readOnlyHint": false,
      "destructiveHint": true,
      "idempotentHint": false
    },
    "groups": [
      "sync",
      "project"
    ],
    "keywords": [
      "merge"
    ]
  },
  {
    "name": "pal_push",
    "title": "Push pal changes",
    "description": "Push local changes to the server (UPDATE). FIRST runs the offline code check (pal_validate) and REFUSES on errors — every error must be fixed before the push can proceed; each finding says exactly how. force:true is drift-only: it can overwrite a newer server marker, but it cannot bypass validation errors. Also refuses if the pal is locked by another person (typed confirmOverride). On success returns the server's save result plus any code WARNINGS.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "force": {
          "type": "boolean"
        },
        "confirmOverride": {
          "type": "string"
        },
        "detail": {
          "type": "string",
          "enum": [
            "summary",
            "normal",
            "full"
          ]
        },
        "maxDiagnostics": {
          "type": "integer",
          "exclusiveMinimum": 0,
          "maximum": 9007199254740991
        },
        "maxBytes": {
          "type": "integer",
          "exclusiveMinimum": 0,
          "maximum": 9007199254740991
        },
        "includePassing": {
          "type": "boolean"
        },
        "includeDebug": {
          "type": "boolean"
        }
      },
      "$schema": "http://json-schema.org/draft-07/schema#"
    },
    "annotations": {
      "readOnlyHint": false,
      "destructiveHint": true,
      "idempotentHint": false
    },
    "groups": [
      "sync",
      "project"
    ],
    "keywords": [
      "push"
    ]
  },
  {
    "name": "pal_lock",
    "title": "Acquire pal lock",
    "description": "Acquire the pal lock. Reports the real holder (from teamInfo) when blocked; override is high-friction and typed.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "confirmOverride": {
          "type": "string"
        }
      },
      "$schema": "http://json-schema.org/draft-07/schema#"
    },
    "annotations": {
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": true
    },
    "groups": [
      "project"
    ],
    "keywords": [
      "lock"
    ]
  },
  {
    "name": "pal_unlock",
    "title": "Release pal lock",
    "description": "Release palsync's lock. Cannot release a PalBuilder checkout — that must be released in PalBuilder.",
    "inputSchema": {
      "type": "object",
      "properties": {},
      "$schema": "http://json-schema.org/draft-07/schema#"
    },
    "annotations": {
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": true
    },
    "groups": [
      "project"
    ],
    "keywords": [
      "unlock"
    ]
  }
]
