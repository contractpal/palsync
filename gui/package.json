{
  "name": "palsync-gui",
  "version": "0.5.0",
  "private": true,
  "description": "Electron GUI wrapper for palsync — workspace/tab manager over real agent-CLI consoles.",
  "main": "src/main/index.js",
  "scripts": {
    "dev:renderer": "vite",
    "dev": "concurrently -k -n renderer,electron -c cyan,yellow \"npm:dev:renderer\" \"electron .\"",
    "build:renderer": "vite build",
    "build:win": "node scripts/bumpVersion.js && npm run build:renderer && node scripts/writeBuildInfo.js && electron-builder --win && node scripts/afterWinBuild.js",
    "build:win:dir": "node scripts/bumpVersion.js && npm run build:renderer && node scripts/writeBuildInfo.js && electron-builder --win --dir",
    "build:mac": "node scripts/bumpVersion.js && npm run build:renderer && node scripts/writeBuildInfo.js && electron-builder --mac",
    "build:mac:dir": "node scripts/bumpVersion.js && npm run build:renderer && node scripts/writeBuildInfo.js && electron-builder --mac --dir",
    "build:linux": "node scripts/bumpVersion.js && npm run build:renderer && node scripts/writeBuildInfo.js && electron-builder --linux && node scripts/afterLinuxBuild.js",
    "build:linux:dir": "node scripts/bumpVersion.js && npm run build:renderer && node scripts/writeBuildInfo.js && electron-builder --linux --dir",
    "postinstall": "patch-package && node scripts/fix-electron-install.js && electron-rebuild -f -w node-pty"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/xterm": "^5.5.0",
    "node-pty": "^1.0.0",
    "palsync": "file:..",
    "qrcode": "^1.5.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@electron/rebuild": "^3.6.1",
    "@vitejs/plugin-react": "^4.3.1",
    "concurrently": "^8.2.2",
    "electron": "^32.0.0",
    "electron-builder": "^25.0.0",
    "patch-package": "^8.0.0",
    "vite": "^5.4.0"
  },
  "build": {
    "appId": "com.contractpal.chip-pal-builder",
    "productName": "Chip Pal Builder",
    "directories": {
      "output": "dist"
    },
    "afterPack": "scripts/afterPack.js",
    "files": [
      "dist-renderer/**/*",
      "src/main/**/*",
      "package.json",
      "build-info.json",
      "!node_modules/palsync/gui/**",
      "!node_modules/palsync/.git/**"
    ],
    "win": {
      "target": "nsis",
      "icon": "build/icon.png"
    },
    "nsis": {
      "artifactName": "ChipPalBuilder.exe"
    },
    "mac": {
      "target": [
        "dmg",
        "zip"
      ],
      "category": "public.app-category.developer-tools",
      "hardenedRuntime": true,
      "gatekeeperAssess": false,
      "icon": "build/icon.png"
    },
    "linux": {
      "target": "AppImage",
      "artifactName": "ChipPalBuilder.AppImage",
      "category": "Development",
      "icon": "build/icon.png"
    }
  },
  "allowScripts": {
    "electron@32.3.3": true,
    "esbuild@0.21.5": true,
    "node-pty@1.1.0": true,
    "file:../..": false,
    "fsevents": true
  }
}
