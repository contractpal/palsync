# dependencies
node_modules/

# logs
*.log

# secrets / credentials — never commit
.palpushrc
.env
.env.*
*.pem
*.key
secrets.json

# per-workspace sync state (generated by the launcher; may name a cloud/user)
.palsync.json
.mcp.json

# pulled pal data — real client/dataset content; must never ship
.pals/
*PAL_BACKUP*
ISR_PAL_BACKUP/

# local / temporary pal workspaces and test output
tmp/
temp/
*.tmp
*-workspace/
.DS_Store

# internal: editor config, verification scripts, internal specs — not part of the public tool
.claude/
scripts/
SPEC_*

# vendored palbuilder platform source — local contract reference only, not part of the CLI
com/

# CCE (code-context-engine)
# CCE local cache (per-machine, not for version control)
.cce/
# Claude Code local settings written by cce init
.claude/settings.local.json

# palsync upgrade SHA stamp (per-install, written at runtime)
.installed-sha

# eval benchmark workspaces — ephemeral, regenerated by eval/setup-runs.sh
eval/runs/*
!eval/runs/README.md
CLAUDE.md
